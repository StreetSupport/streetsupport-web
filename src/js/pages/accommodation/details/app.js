/* global google */

// Common modules
import '../../../common'

const browser = require('../../../browser')
const ViewModel = require('./viewModel')

let viewModel = new ViewModel()
viewModel.render()